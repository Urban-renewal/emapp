feat(authz): agent capability matrix — edit_project_data canary (D.46, Gate-6)

Per-agent capability set (6 curated JSONB toggles, D.17), manager-controlled,
enforced server-side. This canary wires `edit_project_data` (buildings +
apartments writes) end-to-end; the other 5 capabilities are stored but their
resources stay manager-only until follow-up PRs, so a stored flag grants nothing
prematurely.

Storage (Gate-6 — migration 0041):
- memberships.capabilities JSONB, default all-off except view_owners (D.47).
  Zod source of truth = AgentCapabilitiesSchema (shared-types); the DB $type +
  default mirror it (pinned by D46-DRIFT).

POLICY loosen (Gate-6):
- buildings + apartments create/update/delete: manager-only -> manager+agent.
  The coarse AuthorizationGuard (role-only, can't see capabilities) now admits
  agents; the FINE gate runs in the service. policy.spec.ts pin updated. owners
  stays manager-only (owner<->project scoping is a follow-up).

Fine gate + GUARDRAIL:
- requireAgentCapability(tx, user, cap) (common/authz): manager no-op; agent
  needs the flag === true; else 403. Wired into ALL SIX write methods
  (buildings/apartments create/update/archive) AFTER the assigned-project
  scoping. The by-id update/archive paths (manager-only before, so they lacked
  an agent-assignment check) now ALSO call assertProjectVisible — so loosening
  the cell opens no ungated side door. The JWT carries role only, so the
  capability is loaded per-request from memberships inside withTenant.

Manager toggle:
- PATCH /members/:userId/capabilities (manager-only via @AuthzResource('members')),
  subset-merge onto the stored set, agent-only target (else 400
  capabilities_agent_only), audited (member.capabilities_change). capabilities
  added to the Member response (OPTIONAL for add-only cross-track safety; BE
  always populates it). No session revocation needed (caps are never in the JWT).

Evidence (D.51, deterministic real-DB) — agent-capabilities.spec:
- assigned agent WITHOUT edit_project_data -> 403 (capability gate, assignment
  passes); manager toggle flips the SAME agent blocked->allowed (create+update+
  archive); agent WITH flag on UNASSIGNED project -> 404 (assignment gate holds,
  no side door); manager always writes; toggle agent-only -> 400; fresh-agent
  stored caps == shared-types default.

lint + typecheck green. Full db (26 files) + api (61 files) suites passed — no
regression on existing buildings/apartments/members tests.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
