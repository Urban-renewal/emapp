// T1.8 — PII encryption tests — implemented in P1.5 + P1.13
import { describe, it } from 'vitest';

describe('PII encryption (T1.8)', () => {
  it.todo('national_id is stored as encrypted bytea, not plaintext');
  it.todo('phone is stored as encrypted bytea, not plaintext');
  it.todo('decryption returns original value');
});
