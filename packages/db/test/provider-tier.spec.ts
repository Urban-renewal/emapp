// T1.6 — Provider tier access tests — implemented in P1.13
import { describe, it } from 'vitest';

describe('Provider tier (T1.6)', () => {
  it.todo('withProvider records audit row on every call');
  it.todo('provider_users table is not accessible via app_user role');
});
