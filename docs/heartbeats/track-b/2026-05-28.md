# Track B heartbeat — 2026-05-28

- **Wave 4 PR C (M-1 tenant_sessions) shipped + merged.** PR #152 — migration 0038
  (`tenant_sessions` table, RLS-exempt matching `auth_sessions` per D.21),
  Drizzle schema, OTP service mint with `sid` claim + row insert,
  `TenantAuthGuard` revocation check via `isTenantSessionActive`,
  `POST /api/v1/portal/logout` endpoint, TTL drop 30→10 min.
  Audit row uses `actor_type='system'` (the `audit_log_actor_type_valid`
  CHECK constraint rejects `'tenant'`). All 4 new M-1 specs green +
  all 6 S4 specs still green (10/10 portal tests).

- **Wave 4 closeout (PR #153).** All four user-decision items from
  `docs/audits/wave4-pending-user-decisions.md` now resolved
  (H-3 kept · M-1 shipped · C-2 inline · M-7 deferred to V12).

- **Track A unblocked — A.S12 (WeekCalendar) and A.S15 (Export FE).**
  B.S6 + B.S7 (calendar + ICS email) and B.S10 (export endpoint) have
  all been on `main` since this session's first wave. Track A can now
  pick them up. API contracts they need:
  - **A.S12 / Calendar list**: `GET /api/v1/tasks?scheduled_from=ISO&scheduled_to=ISO`
    returns the existing `{ data: Task[], page: {...} }` envelope (D.16). Each
    task now carries `scheduled_at: string | null` and `location: string | null`
    plus external_attendees via a separate `task_external_attendees` row set
    (D.41). Per-task ICS download (read-only — no mutate): `GET /api/v1/tasks/:id/ics`
    returns `text/calendar` with `Content-Disposition: attachment; filename="task-<id>.ics"`.
  - **A.S12 / Calendar create/update**: existing `POST /api/v1/tasks` +
    `PATCH /api/v1/tasks/:id` accept `scheduled_at` + `location` + `external_attendees`
    (string[]). On create/update with non-empty attendees, ICS is emailed via
    Resend automatically (B.S7); `ics_sent_at` lands on the response row.
  - **A.S15 / Export FE**: `GET /api/v1/projects/:id/export?format=xlsx|pdf`.
    Org JWT only. Throttled 10/hour. Hebrew filename via RFC 5987
    `filename*=UTF-8''<percent-encoded-project-name>.<ext>` with ASCII
    `filename="project-<id>.<ext>"` fallback. Streams binary directly —
    consume with `fetch` + `blob()` or `URL.createObjectURL` for download UX.

- **Export audit pass + Wave 5 shipped + merged (all 5 CRITICALs same day).**
  3 parallel auditors (redteam + perf + errors) on `apps/api/src/modules/export/*`
  produced 30 findings (5 CRITICAL). All 5 landed as fix-PRs and merged:
  - **#155** EXP-C1 (CRITICAL redteam) — xlsx formula injection
    neutraliser. Owner names starting with `=`/`+`/`-`/`@` no longer
    become live formulas exfiltrating PII via `=WEBSERVICE(...)`.
  - **#156** E-C3 (CRITICAL errors) — D.16 envelope `message` now
    present on the three export `*Exception` throws.
  - **#157** F1 (CRITICAL perf) — singleton Chromium for PDF. The
    "more than 1 second is excessive" rule was being silently breached
    on EVERY pdf call (1-2.5s cold start per request); now ~500ms warm.
  - **#158** E-C1 + E-C2 (CRITICAL ×2 errors) — split audit into
    `project.export.requested` (pre-flight gate) + `delivered`/`failed`
    (post-render outcome); pre-extract projectName + null out
    cleartext-bearing input before headers flush.
  - **#154** post-#151 regression — imports.s8 A6 spec now filters
    audit rows by action (was assuming single row per target).

  Closeout doc: `docs/audits/2026-05-28-export-wave5-closeout.md`.
  Wave 6 (7 HIGHs) ready to start now that #155-#158 are on main.
