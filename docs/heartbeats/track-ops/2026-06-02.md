- **Track Ops — release-engineering + devex burst (post-#213).** Owner-directed
  maintenance after the D2 close-out (#213). Everything below is on `main` unless
  noted. Discoverable via `gh pr list --state merged` + `git log`, summarised here
  so a new agent has the narrative without reverse-engineering it.
  - **#214** `fix(turbo)` — `SIGNATURE_TOKEN_SECRET` + app secrets were filtered
    by turbo strict-env (not in `globalEnv`) → API boot crash + FE "wrong
    credentials" for all users (the proxy 500'd). Added the missing vars to
    `turbo.json` `globalEnv`.
  - **#218** `fix(web)` — org login served only the manager; removed the cosmetic
    4-role picker so agent/viewer can sign in at `/he/login`.
  - **#219** `feat(dev)` — hard-gated **dev auth bypass**: fixed code `000000`
    for tenant OTP + provider MFA. Double-gated (`NODE_ENV==='development'` AND
    `DEV_AUTH_BYPASS==='1'`), boot-crash if set in production, security-reviewer
    PASS. See `apps/api/src/common/dev-auth-bypass.{gate,}.ts` + `docs/LOCAL-DEV.md`.
  - **Audit gate unblock (vitest CRITICAL GHSA-5xrq-8626-4rwp).** A new advisory
    on `vitest <4.1.0` red-gated EVERY PR (`pnpm audit --audit-level=high`).
    pnpm 9 can't allowlist, so the fix was the bump itself:
    - **#219 (folded in)** vitest 2→4 + **vite 5→6** (required peer) across all 7
      workspace package.jsons + a `pnpm.overrides.vite ^6`. Exposed a latent
      `@emapp/shared-types` typecheck gap (missing `@types/node`) — fixed.
    - **#221** dependabot ignores `vite` major (tracks with vitest, manual PR).
    - **#222** dev-deps group (tsx/turbo/ts-loader) — the safe bumps freed once
      the vite major was ignored.
    - **#224** prod-deps hardening: pin `pg` (a prod-deps bump floated pg to
      8.20+8.21 → duplicate `drizzle-orm` SQL<unknown> → typecheck fail; the
      override forces one copy), ignore `lucide-react` pre-1.0 **minors**, ignore
      dead `better-auth`. Closed the broken grouped prod-deps PR #223.
    - **#226** removed the dead `better-auth` dependency (0 imports, D.21).
  - **#227 (OPEN)** `fix(web)` — **proxy strips `Expect` + hop-by-hop request
    headers.** Root cause of a "every load fails / login 502" report: the
    `app/api/[...path]/route.ts` reverse proxy forwarded `Expect: 100-continue`,
    which undici's `fetch()` rejects (`NotSupportedError`) → 502 on every
    POST/PUT/PATCH. GET worked, so it masqueraded as an auth problem.
    security-reviewer PASS; 2 regression specs; closes a request-smuggling
    primitive too. **Awaiting CI + owner merge.**
  - **Local dev environment (the big devex win).** Stood up local Postgres
    (`postgres:1234@localhost:5432/emapp`, ~1 ms RTT vs ~180 ms remote Neon →
    `/me` 1.4 s → 0.2 s). Launcher `start-dev-local.ps1` (gitignored) uses the
    `-Inner` pattern to pin `DATABASE_URL` to local AFTER Infisical injection
    (Infisical overrides pre-set env vars — the #1 footgun). Full runbook +
    per-role credentials in **`docs/LOCAL-DEV.md`** (new, committed).
  - **`seed:demo`** (this PR) — `packages/db/scripts/seed-demo.ts`: idempotent
    rich single-org dataset (6 projects across all D.18 statuses, ~36 apts mixed
    outreach status, ~40 PII-encrypted owners + ownerships summing to 100,
    documents, signature requests/signatures mixed, contractors + shares with the
    D.46 JSONB matrix, tasks + assignees, notifications, project assignments).
    Lets a new dev see "an org mid-workflow" across every entity.
  - **Known local-only state (NOT in git, a fresh machine won't have it):** the
    demo seed rows (local db + accidentally a copy in remote Neon dev, labelled
    `(דמו)`), the `provider@local.dev` password reset to `DevPassword123!`, and
    the gitignored `*.ps1` launchers. `docs/LOCAL-DEV.md` documents how to
    reconstruct all of it.
