- **Track A (V12) — PERF-1: withTenant round-trip OVERHEAD regression gate (canary).** Locked the historical 4–6 → 3 round-trip collapse as a permanent CI gate: new `packages/db/test/perf-1-withtenant-roundtrips.spec.ts` counts real `client.query()` round-trips and asserts (a) **≤3 overhead** round-trips for a single-statement read (`BEGIN;SET ROLE` + parameter-bound `set_config` + COMMIT) and (b) the **encryption key never appears in query text** (§10.3). Proven bidirectional: gate goes RED at 4 overhead (demo: splitting `BEGIN`/`SET ROLE`) and RED if the key is inlined. The wrapper code was already at the safe floor — no code change; this nails it down so any regression to 4+ or any key-inlining is blocked forever.
- **Decision needed → owner chose Option 2 (keep §10.3, land at 3).** Investigated dropping to ≤2: it requires merging session setup into one simple-protocol multi-statement, which cannot carry the parameter-bound key — forcing either (1) inlining the key into SQL text (rejected by owner: PII key in logs, §10.3 + ISO breach) or (2) a once-per-connection **session-level** key set. I built #2 first and it FAILED mechanically: `DATABASE_URL` is the Neon **`-pooler`** (transaction-pooling) endpoint, so a `set_config(...,false)` outside a tx persists on the pooled backend and **leaked the key to a sibling `providerPool` checkout** — caught by `with-provider-encryption #5`. Real PII-key leak, reverted. Conclusion recorded: ≤2 is not safely reachable under §10.3 + the Neon pooler; 3 is the floor. Absolute latency is a T6 (colocation) concern, not code.
- **Evidence (mechanical, per D.51):** RED→GREEN on the gate (overhead 4→3); EXPLAIN under the path shows `Index Only Scan using idx_owners_org_created_desc`, `Index Cond: org_id = current_setting('app.organization_id')` (1.2ms, no Sort). Full `@emapp/db` suite green single-threaded (25 files / 171). code-review + security-review: no CRITICAL/HIGH.
- **Env note (not blocking; CI-safe):** `with-provider-encryption #5` flakes ONLY when the full suite runs with file-parallelism against the **shared Neon pooler dev DB** under load — a known parallel-pollution artifact (no session-level GUC exists anywhere in code; verified). It passes in isolation, with 2 files, and single-threaded. CI runs against a **local Postgres** (no pooler) so it does not reproduce there. My new file (a 25th parallel fork) is what tips the local race; the gate itself is environment-independent.
