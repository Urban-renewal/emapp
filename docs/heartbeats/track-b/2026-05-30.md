# Track B heartbeat — 2026-05-30

- **V12 FE-fix queue COMPLETE (slices 1–4 merged).** All four shipped, each a
  root-cause fix with a mechanism-based red→green test, full e2e green, and
  code-review + security-review PASS, auto-merged after the 10 CI checks:
  - **FUNC-4** wizard hydration race → PR #175 (commit d15eac1)
  - **FUNC-1** owner optional email/phone + 12-form sweep → PR #176 (0ba636b)
  - **FUNC-3** bare /buildings + /apartments → projects hub → PR #177 (c995e6f)
  - **PERF-4** stop retrying definitive errors (7–9s → <1.5s) → PR #178

- **PERF-2 (getMe SSR self-hop) — DEFERRED to the infra/deploy layer, to be
  done with T6 (owner decision, 2026-05-30: option D). Track B does NOT build
  it.** Root cause confirmed and recorded for the receiving track:
  - `getMe`/`getProviderMe` run server-side and fetch `${origin}/api/v1/me`
    (their OWN Next proxy route via `selfOrigin()`), which then forwards to
    `API_BACKEND_URL`. The self-dependency deadlocks the dev server under load
    and adds a double-hop (~460ms dev); there is a 15s SSR timeout but no
    faster fast-fail.
  - No NEW BE endpoint is required, but every clean fix crosses a boundary
    outside the FE-fix surface, which is why it was escalated rather than
    shipped:
    - **Self-hop bypass** (getMe → `API_BACKEND_URL` direct) is the true root
      fix, but it's a novel change to the deliberately-centralized
      "single backend-hostname location" + `selfOrigin` (§v9-H-1) security
      boundary, and its prod behavior (Cloudflare Pages RSC → private Railway
      URL) can't be validated from a dev session — squarely infra/deploy.
    - A short-TTL session-validity **cache** would widen the token-revocation
      window (auth-freshness regression intersecting the open redteam-H-1).
    - A timeout-only tweak is symptom-not-root (D.51-weak).
  - Handoff note for infra/T6: the proxy route (`app/api/[...path]/route.ts`)
    is the single place that touches `API_BACKEND_URL` by design; any bypass
    must preserve the `selfOrigin` allowlist intent and be verified against the
    CF-Pages→Railway prod topology. Verification target (from FINDINGS-REGISTER
    PERF-2): SSR TTFB on an authenticated page asserts no per-render self-hop
    (or cached) + fast-fail under a stalled /me.

- **Next:** Track B V12 FE-fix backlog is empty. Awaiting new assignment.
