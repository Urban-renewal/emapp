# DV — Orchestrator verification log

> "Don't make me verify you." This is where the orchestrator GATES each
> investigator's findings before they reach the owner: confirm / sharpen /
> refute, with the orchestrator's own evidence. No verdict is relayed unverified.

## persona-manager (PR #236) — gated 2026-06-03

| Finding              | Agent verdict                                                                                                                                                      | Orchestrator gate                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Result                                                                                                                                    |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| **DV-MGR-DOCS**      | "documents path broken, strict `z.array(DocumentSchema).parse` throws on schema drift → can't send to signature"                                                   | Verified the API row shape + the schema. API returns docs with `type ∈ {agreement, blueprint, regulation}`; FE `DocumentTypeEnum = {contract, permit, id_document, floor_plan, financial, other}` — **DISJOINT** → `z.enum` rejects every row → parse throws. **CONFIRMED + sharpened to 3 layers:** (1) taxonomy drift — the doc-type vocabulary is incoherent; the seed/product types (regulation/blueprint/**agreement**, the urban-renewal-real types) are NOT in the FE enum, and **"agreement" — the core signature document — is missing from the picker** (a what's-missing gap); (2) `documents.type` is free `text` in the DB (`artifacts.ts:28`) with NO BE enum → nothing prevents drift; (3) the FE parse is fragile — one bad row throws the WHOLE list and TanStack swallows it (no error UI) → invisible failure. Prod nuance: a real UI-created doc uses a valid enum type → parses; but any seeded/imported/migrated doc with a non-enum type silently hides ALL documents. | **REAL (HIGH).** Blocks the core signature workflow in any demo/seed env + a latent prod fragility + a product gap (no "agreement" type). |
| DV-MGR-OWNER-ACTIONS | "4 owner quick-actions are disabled placeholders (dead no-ops)"                                                                                                    | Plausible, consistent with the DV-ORG-9 / DV-AGENT-CREATE class (controls shown but not wired). Not independently re-driven yet — LOW UX, batched for the polish pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Accept as LOW (UX).                                                                                                                       |
| 13 "working" actions | create project/owner/building/apartment, ownerships=100, task create+edit, note, assign-agent, reveal-PII (audited), archive, notifications mark-read, project-nav | The agent confirmed persistence via API (redirected vs persisted separated) + ran twice stable + hardened the harness against dev-server transients. Method is sound (this is the action-level standard). Spot-accepted; reveal-PII independently corroborated earlier.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Accept (method credible).                                                                                                                 |

## persona-agent (PR #237) — gated 2026-06-03

| Finding                              | Agent verdict                                                                                                            | Orchestrator gate                                                                                                                                                                                                                                                                                                                                                                  | Result                                   |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- |
| **DV-AGENT-NAV** (my own prior HIGH) | "REFUTED — agent opens its projects, 3 clean runs; prior dead-click was a `getByText().click()` hydration-race artifact" | **STRUCTURALLY CONFIRMED REFUTED.** `projects/page.tsx:181` renders the card as a plain `<Link href={\`/projects/${p.id}\`}>`— **ungated, no per-role mechanism** to be dead. It navigates for ANY role. My earlier DV-AGENT-NAV (and my`dv-reality-compare` "manager works/agent doesn't") were BOTH hydration-race artifacts — I made the exact render/timing mistake I flagged. | **DV-AGENT-NAV = FALSE. CLOSED.** Owned. |
| Agent authz boundary                 | "clean — unassigned project → 404 (no leak), members/audit → 403, edit → 403, PII masked (view_owner_pii=false)"         | Consistent with policy.ts (agent not in those write cells) + the static review. Plausible + matches the structural model. Accept; will be re-confirmed in the cross-entity pass.                                                                                                                                                                                                   | Accept (clean boundary).                 |
| DV-AGENT-CREATE                      | "UX leak only — create control shown to agent, POST /projects → 403"                                                     | Reclassify: the control is a `<Link href="/projects/new">` → it navigates to the form (not 'dead'), but the agent can't SUBMIT (403). Same UX class as DV-ORG-9 (controls shown a role can't complete).                                                                                                                                                                            | LOW UX (hide for agents).                |
| ground-truth corrections             | agent assigned to **3** projects (not 2); seed agent `view_owner_pii=false`                                              | Accept — both are factual corrections to my earlier numbers.                                                                                                                                                                                                                                                                                                                       | Noted.                                   |

**Verdict on the agent role:** NOT broken. Navigation works, scope + authz are clean. The "mess" the owner felt is more likely: the documents bug (DV-MGR-DOCS), the dev-server transient degradation, and dead/unwired controls — NOT a broken agent role. MISSING (real gaps): no field-agent home/worklist, no mobile signing flow, an invisible capability model.

## persona-viewer (PR #238) — gated 2026-06-03

| Finding                     | Agent verdict                                                                                                                                                                                                                                                                     | Orchestrator gate                                                                                                                                                                                                                                            | Result                                  |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------- |
| Viewer write attempts       | "SECURITY CLEAN — all 20 mutating actions (create/archive/edit/delete/assign/reveal-PII/sign-cancel/contractor/note/doc/import) BE-blocked 403, nothing persisted (project count 13→13 invariant), each reproduced ≥2×; only 200 = notifications/read-all (self-scoped, correct)" | Method is the gold standard: actual writes attempted + persistence invariant confirmed + raw-API pre-flight + 2 reproductions/write. Accept — viewer is genuinely read-only at the BE.                                                                       | **SECURITY CLEAN. 0 authz bugs.**       |
| **DV-ORG-9** (was MED-HIGH) | "create-project wizard fully renders for viewer; ~30 dead write controls across 12 surfaces"                                                                                                                                                                                      | Confirmed the controls are shown but every one 403s with no mutation → **NOT security.** **DOWNGRADE DV-ORG-9 from MED-HIGH to LOW (UX/polish):** annoying (read-only role looks writable, dead-ends), not a breach. Fix = role-gate the controls in the FE. | **LOW (UX/polish).** Reclassified down. |

## persona-provider (PR #239) — gated 2026-06-03

| Finding                                                                                                                                                                        | Agent verdict                                                                                                                              | Orchestrator gate                                                                                                                                                           | Result                                                  |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| "Provider console FUNCTIONS — onboard creates org (POST 201, appears in tenants, 2 orgs), suspend/reactivate work (suspendedAt set/cleared), tabs load, gate enforces; 0 bugs" | Refutes the owner's "nothing works"                                                                                                        | Method rigorous (POST 201 + fresh `/provider/tenants` re-read + 2× repro + suspendedAt confirmed). Aligns with the BE onboard/suspend endpoints existing + working. Accept. | **WORKS (proven). Owner claim refuted in a clean run.** |
| DV-PROV-AUDIT (new)                                                                                                                                                            | "provider_audit_log has NO read endpoint; /provider/audit shows only org-tier audit_log → operator can't review their own audited actions" | Real gap — for a tier whose entire security story is "every provider action is audited," the absence of an in-product read path is a compliance/UX hole.                    | **MED (missing — security/compliance).**                |

## ⚠️ META-FINDING — the dev-server degradation confounds everything (and likely explains the owner's experience)

**Two owner reports (agent "no access", provider "nothing works") both REFUTE in clean, reproduced runs. The common cause is almost certainly the dev-server instability (PERF-2):** `next dev` lazy-compiles routes and deadlocks/500s under load — every persona agent hit transient 500s + false "action failed" readings, and my own DV-AGENT-NAV false-positive came from the same instability (a hydration race). **A real user hitting the app while it's degraded sees "everything is broken" — even though the features function in a clean run.**

This does NOT dismiss the owner's experience — it's real, and it's a real problem (you cannot reliably demo in dev mode). But it means: **the "nothing works" feeling = dev-server degradation, NOT broken features.** The REAL persistent bugs are the ones that reproduce cleanly: DV-MGR-DOCS (HIGH), DV-ORG-2 (KPI), DV-ORG-1 (jargon), DV-ORG-9 (dead controls, LOW), DV-PROV-AUDIT, locked nav.

**To definitively separate dev-flakiness from real bugs → a PRODUCTION-BUILD pass** (`next build && next start` + `nest build` + local DB — PERF-6): no lazy-compile, no deadlock, representative of what a customer actually gets. Recommended before the product-owner review's final verdict.

**Method note (carry forward):** the dev server degrades under heavy Playwright load (lazy compile / transient chunk 500s) → FALSE "action failed" readings. The `getByText().click()` hydration race is the other false-fail source (it produced my wrong DV-AGENT-NAV). Every "broken" verdict must be reproduced ≥2×, wait for hydration, and be distinguished from a transient 500 / a structural impossibility. The orchestrator re-checks every HIGH finding against the code structure.
