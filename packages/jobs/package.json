{
  "name": "@emapp/jobs",
  "version": "0.0.0",
  "private": true,
  "description": "EMAPP background-job contracts — job names, payload Zod schemas, and the IJobHandler interface. Imported by both @emapp/api (enqueue side) and @emapp/worker (consume side). Single source of truth for the queue boundary.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "vitest": "^4.1.0"
  }
}
