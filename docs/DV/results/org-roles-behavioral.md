# DV results — Org roles (agent / viewer) — BEHAVIORAL (2026-06-02)

> Ran `dv-org-{agent,viewer,gaps}.spec.ts` headless against the (restarted) local
> stack. App stayed up through all 3 (uptime 17→453s — no deadlock this round).
> Evidence: `artifacts/org-{agent,viewer,gaps}-evidence.json` + per-spec PNGs.

## Verdicts

| ID           | Sev                           | Role/area                 | Verdict                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Evidence                                             |
| ------------ | ----------------------------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| DV-ORG-5/6/7 | —                             | **agent**                 | ✅ **scoping CORRECT** — spec passed. Agent sees only assigned projects, no members/audit/settings nav, no cross-scope render.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | dv-org-agent.spec passed (3.0m)                      |
| **DV-ORG-9** | **MED-HIGH (UX/correctness)** | **viewer**                | 🟥 **Viewer UI is NOT read-only.** It renders ~30 mutating controls across 12 surfaces: "פרויקט חדש"/"צור פרויקט", "ארכוב" (building/apartment/owner detail), "ערוך חלוקה", "הוספת בעל דירה", "צור בקשה חדשה", "**חתום**" (many on sigreq-list), "הוספת קבלן", "צור משימה", "צור הערה". The BE policy matrix excludes viewer from every write cell → clicks 403 (so NOT a security breach) — but every write control is a **dead-click**, and the read-only role looks fully writable. **Fix: role-gate the write controls in the FE (hide/disable for viewer), same pattern as DEF-3.** Verify the BE actually 403s each (esp. "חתום"/sign) — any that DON'T = escalate to security. | dv-org-viewer.spec failed with the full control list |
| DV-ORG-13    | INCONCLUSIVE (spec bug)       | cross-tenant (Alpha↔Beta) | ⚠️ The gaps spec compared **page chrome** ("פרויקטים", "צור פרויקט", "בעלי דירות") which is identical in both orgs → false "overlap". It did NOT compare **data rows** (project names / owner names). Behavioral isolation is therefore **unverified**; static analysis says RLS-enforced. **Re-test:** assert on the data rows (project titles, owner names), not the static UI labels.                                                                                                                                                                                                                                                                                              | dv-org-gaps.spec false-positive                      |
| DV-ORG-12    | (inventory correction)        | org-switcher              | There is **no org-switcher** — the topbar org name is a static label; multi-org isolation is by separate accounts + RLS. My INVENTORY assumed a switcher; corrected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | static (topbar.tsx)                                  |
| DV-ORG-15    | OK                            | confirm-archive           | Archive trigger opens a cancellable confirm dialog (the spec's "failure" is its own assertion framing; the dialog behaves).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | dv-org-gaps                                          |

## Headline

**DV-ORG-9 (viewer not read-only)** is the real finding from the role pass — and exactly the "what's planned ≠ what happens" class: viewer is _designed_ read-only, but _renders_ fully writable. Needs FE role-gating + a BE-403 confirmation sweep (the sign buttons especially).
