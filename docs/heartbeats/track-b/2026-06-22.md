# Track B — 2026-06-22 (autonomous — autonomy engine producers)

- G1 TaskWatcher SHIPPED (PR open): the system DETECTS task-worthy conditions and
  DRAFTS a `task.create` PROPOSAL into the Approval Inbox; manager APPROVES → a
  SYSTEM-OWNED task is created via the EXISTING gated `tasks.create` (verbatim,
  propose-and-confirm — no auto-execute).
- Condition chosen: a `gathering_signatures` project MISSING a required document type
  for its renewal track (reuses `REQUIRED_DOC_TYPES_BY_TRACK` — agreement/land_registry/
  blueprint, + regulation for pinui_binui). One `task.create` per (project, missing type).
- Engine drop-in (no new part): `TaskWatcher` IRecommender (set-based detection across
  ALL orgs in ONE query; non-PII columns only) → generic ProposalProducer (per-producer
  isolation) → `task.create` executor in ProposalsService (classify-recheck + manager-only
  - reuse `tasks.create` verbatim with an internal system-origin stamp).
- New `task.create` kind in AutonomyPolicy (internal+reversible+non-PII+non-outbound →
  autoExecute-ELIGIBLE but routed to PROPOSE; same pattern as signature_request.reissue).
- Migration 0082 (additive): `tasks.source` ('user'|'system' CHECK) + `tasks.origin_ref` +
  a partial-unique `(org_id, origin_ref) WHERE source='system' AND open` that dedups
  auto-create + anchors the future auto-close. Journal `when` 1783800000000 > prior max.
  ⚠️ RENUMBER vs held #498 on merge (latest applied here = 0081).
- Voice law honored: task copy frames the user's situation ("חסר מסמך נדרש: נסח טאבו —
  מומלץ להשלים"), never system-first-person; PII-free (doc-type label only).
- Tests (vs local PG): TaskWatcher detection/track/dedup/no-PII/archived×5 · proposals
  executor classify-recheck+task-create+manager-only ×2 · copy composer voice+PII ×4 ·
  autonomy-policy taxonomy 16 (incl. task.create) · proposals+recommenders 16 — all green.
  tc + lint clean across jobs/db/api/worker.
- G-RT independent red-team: all 6 invariants HELD (proposal RLS · task-create authz ·
  no-PII · the boundary · migration safety · dedup race). MED-1 (pre-existing approve gate
  = signature_requests.send; binding gate is requireManager — flagged for a proposals.\*
  permission family follow-on, not a G1 blocker).
- Parent owes: G-RT sign-off + (FE) real-Chrome walk when the Inbox copy lands (no FE in
  this slice). Gate-6 trailer on the PR (adds migration 0082).
