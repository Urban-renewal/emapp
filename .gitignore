node_modules/
dist/
build/
.turbo/
.next/
coverage/
*.log
.DS_Store
.env
.env.local
.env.*.local
.infisical.json
.claude/scheduled_tasks.lock

# Playwright (Phase 4a S11)
test-results/
playwright-report/
playwright/.cache/

# MSW service worker — generated by `pnpm exec msw init public/`.
# Kept out of git so a future commit cannot accidentally ship MSW in
# prod. Developers run the init command locally when they want
# NEXT_PUBLIC_MSW=1 offline dev. §RED-3 closure.
apps/web/public/mockServiceWorker.js

# Local-only dev helpers (contain local dev secrets — never commit)
start-dev-local.ps1
mfa-code.ps1
packages/db/scripts/_tmp_*.ts
