// T1.5 — RLS isolation tests — implemented in P1.11 + P1.13
import { describe, it } from 'vitest';

describe('RLS isolation (T1.5)', () => {
  it.todo('org A cannot read org B records');
  it.todo('RLS is FORCE-enabled on all tenant tables');
  it.todo('anon role sees empty result sets');
});
