# EMAPP Master Dispatch Brief (council 2026-06-26)

> Builder-ready dispatch table + critical-path corrections + conflict/migration map for waves 0-4.
> Companion to docs/BUILD-LEDGER.md. On conflict, the adversarial corrections win.

I have all four design docs plus the sequencer brief inline. This is a synthesis/authoring task — I have the ground truth I need. Let me produce the merged, builder-ready dispatch brief.

# EMAPP — MASTER DISPATCH BRIEF (Waves 0–4, builder-ready)

**Plan of record:** `docs/BUILD-LEDGER.md` + `docs/BLUEPRINT-AUTONOMOUS-MANAGING-SYSTEM.md` (both on `main`). On any conflict between the naive ledger order and the adversarial corrections (P0-1, P0-2, P0-3, P1-1/2/3, P2-1, P2-2, P3), **the adversarial correction wins** (it is verified against real code).

**Critical-path spine (all serial work hangs off this):** `1.1 → 1.2a → 1.2b → {1.5, 1.3}`. The **1.2a executor-extraction is the true bottleneck — resource it first and alone.** Wave 4 is its own serial chain `4.1 → 4.2 → 4.3`, independent of the spine after 1.1, and is **demoted to LAST among build-now work** (P2-1: 100% of its user value is behind the owner gate).

**Three reshaping corrections, applied throughout:**

- **P2-2** — `0.1` blocks nothing (perception _replaces_ the buggy `COUNT(*)`, doesn't read it) → `0.1` runs fully parallel.
- **P0-1** — `1.2` is NOT a one-liner. Split: **1.2a** (extract DI-bound executors → pure `applyProposalEffect(tx,kind,proposal)`) then **1.2b** (auto-apply hook). 1.2a gates every graduation.
- **P3 + P2-1** — insert **1.5 undo-ledger** (gates trust → without it auto-execute never gets switched on) BEFORE Wave 4; demote Wave 4 below Waves 2–3.

---

## 1. DEPENDENCY-ORDERED DISPATCH TABLE

Legend: ✅ build-NOW-and-merge · 🟡 machinery-NOW/send-or-act-GATED · `→` hard-depends-on. One PR per row unless noted "couple". **Migration №** assumes nothing else lands first — **renumber on merge** (verify `_journal.json` `when` strictly increases).

### WAVE 0 — Truth + Speed + Security (independent; fan out wide)

| Seq | Slice    | Goal                                                                                                                                                                                                                          | Key files                                                     | Seam to REUSE                                              | Now/Gated                              | Gate                                                                                                                                                                                                   | Parallel-safe with                                 |
| --- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- | ---------------------------------------------------------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------- |
| 1   | **0.1**  | Home stat single-source: kill bare `COUNT(*)` → agent-branch shape via new `orgStatsSignatureCountsSql`; provider KPI `status IN ('signed','pending')`; residents via building-join + UI note "בעלים בפרויקטים פעילים" (P1-2) | `apps/api/src/modules/projects/projects.service.ts:1404-1410` | `projectSetSignatureDocIdsSql` + agent branch `:1392-1402` | ✅                                     | single-source (KPI === Σ pulse); static ratchet `no-adhoc-signature-count.spec.ts` (clone `app-no-bare-text-muted.spec.ts`); G-RT RLS-unchanged; G-QA @100+; **P1-2 residents-drop legibility in DoD** | everything in W0                                   |
| 2   | **0.2**  | Kill getMe SSR double-hop: `getMe()` reads `API_BACKEND_URL` server-side, drop proxy self-hop (~0.39s/render)                                                                                                                 | `apps/web/.../auth.ts:getMe`                                  | §v9-M-9 (reversible)                                       | ✅                                     | G-QA latency (<1s warm, report ms); G-RT Set-Cookie/header parity; `auth.spec.ts` env guard                                                                                                            | all W0                                             |
| 3   | **0.3**  | Dev DNS `--dns-result-order=ipv4first` on web; document hosts-file fix                                                                                                                                                        | `start-dev-local.ps1`                                         | —                                                          | 🟡 flag NOW · hosts-file GATED (admin) | latency re-measure                                                                                                                                                                                     | all W0                                             |
| 4   | **0.S1** | env fail-open→fail-closed: API Dockerfile `ENV NODE_ENV=production` + resolver kills SMS→Noop / OTP-000000 / invite-no-op / base-url→localhost                                                                                | API Dockerfile + env resolver                                 | #556 fail-closed pattern                                   | ✅ HIGH                                | G-RT (each fail-open path now blocks); fail-closed unit per env key                                                                                                                                    | all W0                                             |
| 5   | **0.S2** | token-in-sinks: Sentry `beforeSend` URL scrub + CF Pages Function logs + `mailErr.message` token                                                                                                                              | Sentry config + CF function + mail err                        | #560 scrub seam                                            | ✅ HIGH                                | G-RT (no token to any sink); regression spec per sink                                                                                                                                                  | all W0                                             |
| 6   | **0.S3** | delivery-outcome honesty: chase toast + external-share "re-issued" + reissue false-WhatsApp + invite "נשלחה" copy                                                                                                             | FE toasts + share copy                                        | `didAnyChannelDeliver`/`no_channel` (#558)                 | ✅ HIGH                                | G-QA OUTCOME (copy matches real delivery state); G-RT                                                                                                                                                  | all W0                                             |
| 7   | **0.S4** | cross-entity authz: archiving contractor/project revokes shares; project-scope resolver leaks apartment docs                                                                                                                  | `external-party-authz.ts` (read), archive paths               | `decideExternalPartyAccess`/`resolveDocumentAccess`        | ✅ HIGH                                | G-RT (loop: try leak every angle until closed); single-source (one resolver)                                                                                                                           | all W0 except L-Party (holds external_share later) |
| 8   | **0.S5** | OTP/calendar: enumeration oracle + non-atomic brute-force lockout + calendar attendee lifecycle                                                                                                                               | OTP service tx/row-lock                                       | OTP service tx                                             | ✅ HIGH                                | G-RT (atomic lockout under concurrency; no enum oracle)                                                                                                                                                | all W0 — **but may need migration token**          |
| 9   | **0.S6** | error legibility: contractor false-empty + portal 5xx→login + docs `error={undefined}` — **EXCLUDES sign-page (→ 2.3)**                                                                                                       | `DataState`; the listed error surfaces                        | `DataState`; transient-vs-terminal split                   | ✅                                     | G-QA technophobe (every empty/error/loading legible); forced-5xx walk                                                                                                                                  | all W0                                             |

### WAVE 1 — Perception + Recommender spine (THE SERIAL SPINE — one builder, strictly ordered)

| Seq | Slice    | Goal                                                                                                                                                                                                                                                                                            | Key files                                                                                                           | Seam to REUSE                                                                                                             | Now/Gated                            | Gate                                                                                                                                           | Parallel-safe with                       |
| --- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- |
| 10  | **1.0**  | Lock catalog + drift-guard: CI fails a PR adding enum/route/recommender not in catalog                                                                                                                                                                                                          | catalog + CI guard                                                                                                  | catalog = source-of-truth                                                                                                 | ✅                                   | guard fails a synthetic drift PR                                                                                                               | all (read-only-ish)                      |
| 11  | **1.1**  | `ProjectPerception` assembler: ONE PII-free per-project set-based reader; home KPI = Σ `perception.signatures`; add `project-perception.ts` type + `AttentionReason` enum + `attentionReasonToActionKind: Record<AttentionReason, AutonomyActionKind \| null>` (P1-1: `\|null` = perceive-only) | `packages/shared-types/src/project-perception.ts`, `packages/db/src/helpers/perception/project-lifecycle.detect.ts` | `signatureProgressByProject` + `computeConsentAggregates` + `detectMissingRequiredDocs` CTE + `PROJECT_TERMINAL_STATUSES` | ✅                                   | single-source (pulse+recommenders consume THIS); G-RT (no PII reachable); **P1-1 `\|null` map**                                                | W2/W3 bursts AFTER it lands              |
| 12  | **1.2a** | **Executor extraction (P0-1, TRUE BOTTLENECK):** DI-bound executors → DI-free `applyProposalEffect(tx,kind,proposal)` in `packages/db`. `approve` + future producer become TWO thin callers. NO behavior change                                                                                 | `apps/api/src/modules/proposals/proposals.service.ts:99-185` → `packages/db`                                        | the executors map; `withTenant` purity                                                                                    | ✅                                   | G-RT (approve output byte-identical pre/post); reuse-verified; full proposals spec green                                                       | **NOTHING** (owns executors exclusively) |
| 13  | **1.2b** | Auto-execute graduation hook: in `runProposalProducerTick`, after `emitProposal inserted:true`, if `classify(kind)==='autoExecute'` ∧ org-opt-in → `applyProposalEffect` + flip pending→applied + system-audit                                                                                  | producer `proposal-producer.ts:160`; `applyProposalEffect`                                                          | `IRecommender`/`emitProposal`/`classify`; `CAMPAIGN_SEND_ENABLED` kill-switch pattern (default OFF)                       | ✅ (per-kind org-opt-in default OFF) | **G-RT focus:** prove NO outbound/PII/floor kind reaches auto-apply (boundary re-assert at execute); every graduated effect reversible+audited | after 1.2a                               |
| 14  | **1.5**  | **"What I handled for you" undo-ledger (P3 — gates TRUST):** manager view of `system-audit` auto-execute rows, instrument-voice Hebrew per row + one-click reverse per kind; + producer rules "rejection cools the dedup key" + `DetectedCondition.reason` human-string required                | `board-primitives`; `buildPulseSentence`; executors map                                                             | `system-audit` rows; executors map (reverse = inverse effect)                                                             | ✅                                   | G-QA technophobe (see/understand/undo in 1 click); G-RT (reverse can't escalate); rejection-cooldown spec                                      | after 1.2b                               |
| 15  | **1.3**  | Recommenders on the spine: `signature-stalled` (`reminder.send`) + `signature-expiring` (`signature_request.reissue`). **P0-2: reissue stays proposeConfirm only** — `reissueAndDeliver` sends; do NOT re-split into dead `reissueExpired`                                                      | new `IRecommender`s; `autonomy-policy.ts`                                                                           | new `IRecommender`s on 1.2b; existing kinds                                                                               | ✅ (propose only)                    | G-RT (consent NOT hardcoded — no #516 class, fail-closed); single-source; **P0-2**                                                             | after 1.2b                               |

### WAVE 2 — Auto-data-in (highest-leverage reachable wave; parallel-safe once spine ≥1.1)

| Seq | Slice       | Goal                                                                                                                                                                                                                                                                                                          | Key files                                      | Seam to REUSE                                                                                                        | Now/Gated | Gate                                                                                                                       | Parallel-safe with                                               |
| --- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | --------- | -------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| 16  | **2.1**     | Generate-building-apartments: "כמה קומות × דירות?" → one confirm generates rows. **Manager-confirmed, does NOT go through classify() (P0-3 — KILL the `building.apartments.generate` autoExecute row; one mechanism)** `POST /buildings/:id/apartments:generate` loops existing create in one `withTenant` tx | buildings/apartments write path                | existing apartments write; natural-sort + `unit_type` defaults                                                       | ✅        | G-QA technophobe @scale (generate 24, verify numbering + EVERY row); **P0-3 single-mechanism**                             | 2.2 ∥ board ∥ search lanes                                       |
| 17  | **2.2**     | נסח→owners de-jargoned: collapse 5-button tabu to ONE "העלה נסח ומלא בעלים"; confidence→badge; + `tabu-autofill-suggest` recommender                                                                                                                                                                          | tabu UI + recommender                          | `tabu.parse.toDraft`→`tabu.confirm.ownerships` (humanOnly §7 floor STAYS); `IRecommender`                            | ✅        | G-RT (mandatory human-confirm before legal write intact; PII in draft never logged); G-QA technophobe                      | 2.1 ∥ board ∥ search                                             |
| 18  | **2.3**     | Schema-vocab de-leak + **sign-page 5xx** (owns it, NOT 0.S6): i18n sweep (D.25/אטומית/doc_scope/מסמכי-ליבה/unit_type/מונה/מכנה→plain Hebrew); sign-page 5xx→retry stage; + i18n lint banning code/snake_case/math-words in user values                                                                        | i18n files; sign page `load()`/`onSubmit`      | i18n only (own namespace); sign page                                                                                 | ✅        | G-QA technophobe (view-source every touched page); **grep `apps/web/e2e/` for old literals before merge**; forced-5xx walk | own i18n window — **not concurrent on same keys as board lanes** |
| 19  | **2.4–2.7** | ➕ future-states — see §1b for full per-slice spec                                                                                                                                                                                                                                                            | per-entity schemas + perception + recommenders | respective entity state-models; `IRecommender` on 1.2b; **all reuse `task.create` — `autonomy-policy.ts` untouched** | ✅        | G-RT per new enum; drift-guard (1.0); single-source per state read; **2.5 `@security-reviewer` mandatory (guardian PII)**  | ONE builder, internally serial (all touch migrations)            |

### WAVE 3 — Flat-wall closure + P1 convergence (parallel-safe once spine ≥1.1)

| Seq | Slice   | Goal                                                                                                                                             | Key files                           | Seam to REUSE                                                                     | Now/Gated           | Gate                                                                            | Parallel-safe with  |
| --- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------- | --------------------------------------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------- | ------------------- |
| 20  | **3.1** | Tasks situation-picture: grouped/attention-first board; render `task.create` proposals + `project.flagAtRisk` badge                              | tasks board                         | `groupByKind`→`groupByDueDate` + `FleetSection`/`ActionCard`/`buildPulseSentence` | ✅                  | G-QA @scale (100s scannable); single-source                                     | 3.2 ∥ data ∥ search |
| 21  | **3.2** | Documents → reuse board-primitives (P1): DELETE re-impl `CockpitPulse`(:327)/`ProjectAttentionCard`(:356); remove scroll "all docs" escape hatch | `documents-list.client.tsx:327,356` | `board-primitives.tsx` (delete the second impl)                                   | ✅                  | **G-RT (P1 reuse verified, zero re-impl)**; single-source (no "0 מתוך X"); G-QA | 3.1 ∥ data ∥ search |
| 22  | **3.3** | Search/filter: imports · messages · notes · audit (audit BE-gated rollout)                                                                       | list endpoints                      | existing list endpoints + keyset cursor                                           | ✅ (audit BE-gated) | G-QA @scale                                                                     | all W3              |
| 23  | **3.4** | State-model on every screen × role (incl. contractor/tenant/agent)                                                                               | role surfaces                       | `board-primitives` + `ProjectPerception`                                          | ✅                  | G-QA per role                                                                   | after 2.4–2.7       |

### WAVE 4 — Cross-party (HARD-SERIAL 4.1→4.2→4.3; DEMOTED; machinery NOW, inert) — full spec §1c

| Seq | Slice   | Goal                                                                                                                                                                                                                                                        | Key files                                                                                                                               | Seam to REUSE                                                                                                                                | Now/Gated                                                    | Gate                                                                                                                                                               | Parallel-safe with                    |
| --- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------- |
| 24  | **4.1** | `party_request` entity + recipient cols + WIRE resolver + `partyTypeForDocumentParty` bridge                                                                                                                                                                | `external-share.ts`, new `party-request.ts`, `shared-types/external-share.ts`, migration `0083`                                         | `decideExternalPartyAccess` (WIRE — already built); `encryptField`; `0079` migration template                                                | 🟡 build NOW · **prod-backfill GATED**                       | G-RT (fail-closed; sensitive exclusion holds; bridge totality tested); single-source                                                                               | NOTHING (owns external_share, serial) |
| 25  | **4.2** | Party token tier (X-S4) + write-back: `PartyTokenService` (audience `emapp-party`, clone `ShareTokenService`) + `PartyAuthGuard` + party read + `documents.actions.upload` cap (default OFF) + `POST /party/requests/:id/fulfill` (scope-derived auto-file) | new `apps/api/.../party-portal/**`, `external-share.ts`, `env.ts`                                                                       | clone `ShareTokenService`; `assertWithinCeiling`/narrows-only; UNCHANGED `decideExternalPartyAccess`/`isDocInShareScope`                     | 🟡 token/guard/write-back NOW · **OTP delivery GATED**       | G-RT (audience can't masquerade; write only within scope — same `isDocInShareScope` on WRITE target; no overwrite)                                                 | after 4.1 (serial)                    |
| 26  | **4.3** | Outbound delivery + autonomous re-chase: chase `send` thunk via `governOutboundSend`; `outbound_ledger.proposal_id` nullable + `cause` discriminator + CHECK + cause-aware lookup (P1-3); `party-request-chase` recommender                                 | `document-chase-executor.ts`, new `party-chase-delivery.ts`, `outbound-ledger.ts`, `outbound-governor.ts`, new recommender + worker reg | `governOutboundSend` (ONLY outbound) + `document.chase.send` + the ONE `outbound_ledger` + `ExternalSharesService` mint + cloned party token | 🟡 **GATED (real send)** — wiring/draft NOW, kill-switch OFF | G-RT (consent fail-closed, no #516; null-cause row can't escape gating); **G-QA OUTCOME (log in AS recipient, ledger 0→1)**; **P1-3 CHECK + cause-lookup in spec** | after 4.2 (serial)                    |

---

## 1b. WAVE 2.4–2.7 expanded (turnkey per-slice)

All four are **additive state dimensions** layered _alongside_ locked enums — **D.18 `project_status` and `apartment_status` are LAW, do NOT touch.** Every new table: `ENABLE`+`FORCE ROW LEVEL SECURITY` + `tenant_isolation` on `org_id` (copy `0079_external_share.sql` template, guarded `DO $$ … pg_policies` block, idempotent `IF NOT EXISTS`), grants `SELECT/INSERT/UPDATE` to `app_user`, `REVOKE DELETE`. Every recommender = a NEW `IRecommender` (no new engine part); **every recommender reuses `task.create` → `autonomy-policy.ts` ACTION_TABLE stays untouched** (verify in PR — that's the SOLID prediction). Each ships propose-only. Closed enums enforced Zod-at-edge **+ belt-and-suspenders DB CHECK**.

| Slice             | Migration                                                                 | Schema                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Perception facet (PII-free)                                                                                                                                                                         | Recommender(s)                                                                                                                                                                                | New autonomy kind?                                 |
| ----------------- | ------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| **2.4 Project**   | `0083` — columns on `projects`                                            | `permitStatus`(none/submitted/approved/rejected/expired)+`permitNumber`+`permitExpiresAt`; `objectionsStatus`(none/open/resolved)+`objectionsDeadlineAt`; `financingStatus`(none/sought/secured/rejected); `onHold bool default false`+`holdReason`+`heldAt`; `finalReviewStatus`(none/in_review/passed). Index `idx_projects_permit_expiry … WHERE permit_status='approved' AND archived_at IS NULL`                                                                                                                                                           | `lifecycle{permitStatus,permitExpiresAt,permitDaysToExpiry,objectionsStatus,objectionsDaysToDeadline,financingStatus,onHold,finalReviewStatus}`                                                     | `permit-expiry-warn` (≤30d → `task.create`), `objection-flag` (≤14d → `task.create`); `financing_blocked`/`ready_final_review` → `null` badge                                                 | **No**                                             |
| **2.5 Owner**     | `0084` — **new `owner_states` table**. **`@security-reviewer` MANDATORY** | `kind`(competency/dispute/transfer/lien/verify/consent_withdrawal)+`subKind`; **guardian PII `*_encrypted bytea`** (`encryptField`, key `PII_ENCRYPTION_KEY`, never logged); `status`(active/resolved); FK `ownerId` CASCADE. Index `… WHERE status='active' AND archived_at IS NULL`                                                                                                                                                                                                                                                                           | **counts only** `{ownersWithActiveLegalState,deceasedCount,disputedCount,lienCount,unverifiedCount,consentWithdrawnCount}` — NO names/guardian PII (the `.strict()` schema is the structural guard) | `ownership-mismatch-flag` (blocking owner counted in threshold → `task.create`, evidence = `ownerId`+`stateKind` taxonomy ONLY)                                                               | **No** (do NOT add a kind that reads guardian PII) |
| **2.6 Document**  | `0085` — columns on `documents`                                           | `legalStatus`(draft/reviewed/approved/rejected, default draft); `versionState`(current/superseded)+`supersededByDocumentId` SET NULL; `validUntil`; `notaryStatus`(none/required/notarized)+`notarizedAt`; `relevantPhase`(planning/signatures/permit/construction/completion). Index `… WHERE legal_status='approved' AND archived_at IS NULL`. **CRITICAL:** sharpen the ONE `detectMissingRequiredDocs` so a requirement is satisfied only by `legalStatus IN('reviewed','approved') ∧ versionState='current' ∧ archived_at IS NULL` — do NOT fork the query | `{docsExpiringSoon,docsRejected,docsAwaitingNotary}`                                                                                                                                                | `doc-expiry-warn` (approved ∧ validUntil ≤30d ∧ current → `task.create`)                                                                                                                      | **No**                                             |
| **2.7 Apartment** | `0086` — **new `apartment_states` table**                                 | `kind`(deceased/dispute/poa/eviction/repairs/rights_transfer); `status`(active/resolved); FK `apartmentId` CASCADE; **no raw PII cols** (person = an `owner`/`owner_state`). Index `… WHERE status='active' AND archived_at IS NULL`                                                                                                                                                                                                                                                                                                                            | `{apartmentsWithLegalState,evictionCount,disputeCount,repairsCount,rightsTransferCount}`                                                                                                            | `apartment-blocker-flag` (OPTIONAL — eviction/dispute/deceased in gathering_signatures → `task.create`; drop to perceive-only `null` if council judges redundant with 2.5, document the call) | **No**                                             |

FE for all: reuse `board-primitives` + `StatusBadge` + `<NameDisplay>`; plain de-jargoned Hebrew via next-intl (enum literals/snake_case NEVER shown raw — 2.3 i18n lint polices it). Owner/apartment guardian PII stays behind the existing step-up/`view_owner_pii` gate.

**Dependency note:** these consume `ProjectPerception` + assembler (already on main) — extend the `.strict()` schemas additively. Recommenders need 1.2b registry to _run_ but can be **built + unit-tested against the `IRecommender` contract now**.

---

## 1c. WAVE 4 expanded (turnkey per-slice)

**Hard-serial 4.1→4.2→4.3; one builder owns `external_share` exclusively for the whole chain.** The authz resolver (`decideExternalPartyAccess`/`isDocInShareScope`/`resolveDocumentAccess`) is **already BUILT and wired** — 4.1 only adds recipient capture + the who-owes surface; **change NOTHING in the resolver.**

### 4.1 — `party_request` + recipient capture + the bridge

- **The `partyTypeForDocumentParty` bridge** (THE one new canonical artifact, declared in `packages/shared-types/src/external-share.ts`): **one-directional, partial, fail-closed** `Record<DocumentParty, ExternalSharePartyType | null>`. The two enums are non-isomorphic (binder "who's responsible" vs share "what kind of counterparty"). Map: `appraiser→appraiser`, `surveyor→surveyor`, `supervisor→supervisor`, `lawyer→tenant_lawyer`, `contractor→developer`; **`municipality/architect/owner/other→null`** (a request for these may be CREATED to track who-owes, but `delivery_channel` can only be `manual_link` — NEVER auto-mints a grant; 4.2/4.3 must hard-reject auto-mint where bridge returns null). Add an exhaustiveness spec.
- **Recipient cols on `external_share`** (migration `0083`, additive, all-nullable, backfill-safe): `recipient_name text` (contact label, NOT PII), `recipient_email_enc bytea` + `recipient_phone_enc bytea` (**pgcrypto via `encryptField`/`decryptField`, key `PII_ENCRYPTION_KEY`** — mirror `owners.national_id_encrypted` customType; `toView()` returns **MASKED only**, never plaintext/ciphertext; audit `afterState` carries `recipientName`+`partyType` ONLY), `delivery_channel` enum(`email`/`sms`/`manual_link`, default `manual_link`), `last_delivered_at timestamptz` (set ONLY by a real governed send in 4.3; NULL until then).
- **`party_request` table** (same migration): `org_id`, `project_id` CASCADE, `document_party text` (the 9 `DOCUMENT_PARTY_VALUES`, tolerant-text written from the validator), `requested_doc_type text`, `status`(open/delivered/fulfilled/cancelled), `external_share_id` SET NULL, `fulfilled_document_id` SET NULL, `fulfilled_at`, provenance. Partial unique `… WHERE status IN('open','delivered')` (one live obligation per project/party/type). RLS FORCE; `REVOKE DELETE` (cancel = status transition).
- **Service:** extend `ExternalSharesService.create/update` to encrypt recipient (reuse `requirePiiKeys` guard); `party_request` CRUD (`createPartyRequest`/`listOpen*`/`cancel`, all `withTenant`, `isOrgSuspended` gate, no-oracle 404). **No delivery/fulfill here.** `last_delivered_at` stays NULL.
- **FE who-owes-what situation-picture:** board-primitives, grouped by project→party, attention-first (`open`+`manual_link` floats up), **honest copy** — `lastDeliveredAt===null` ⇒ "טרם נשלח"/"ממתין לשליחה ידנית", NEVER "נשלח/התקבל" (per #549 honesty fix). `<NameDisplay>` on `recipientName`.
- **GATED:** prod backfill of `recipient_*` on existing live `0079` rows (forward migration ships now; data backfill on live = owner-gated). Nothing sends.

### 4.2 — Party token tier (X-S4) + write-back

- **`PartyTokenService`** (new `apps/api/src/modules/party-portal/party-token.service.ts`): **CLONE** `ShareTokenService` (don't subclass). Audience `emapp-party` (DISTINCT from emapp-share/api/provider/tenant/sign/exchange), HS256, 30d TTL. **Payload binds `{shareId, orgId}` ONLY** — perms/scope/party_type/allow_sensitive are RE-READ LIVE from the row per request (a narrowed/revoked grant must lose authority immediately). Dedicated `PARTY_TOKEN_SECRET` (`env.ts`, `.min(44).optional()`, **OWNER-DEPLOY-gated** Infisical; dev fallback `?? JWT_SECRET`; dual-verify de-dup grace). Closed `PartyTokenFailReason` → generic `{error:{code:'invalid_token'}}` (no expired-vs-forged oracle).
- **`PartyAuthGuard`** (clone `contractor-auth.guard.ts`): cookie `party_access_token` + Bearer fallback; verify → **re-assert `exp` at retrieval** (fail-closed) → load `external_share` under `withTenant(payload.orgId)` with `isOrgSuspended` first, `revokedAt IS NULL` → generic 401 on missing/revoked/suspended. Attach `PartyContext{shareId,orgId,partyType,scopeType,scopeIds,share,otpVerified:false}`.
- **`documents.actions.upload` capability** (default OFF everywhere; keep schema↔Zod byte-equivalent): in `PARTY_PRESET_CEILINGS` set `upload:true` ONLY for `appraiser`/`surveyor`/`developer`/`developer_lawyer`/`tenant_lawyer`/`special_admin`; `bank`/`committee` stay false. Ceiling validator **REJECTS (not clamps)** an over-ask. Migration `jsonb_set` backfill sets absent key to `false`.
- **READ endpoints** `/api/v1/party/*`: `GET /overview`, `GET /documents` (resolve each doc through `decideExternalPartyAccess` — CALL, don't rebuild; only `allow:true`), `GET /documents/:id/download` (same resolver; `requiresDecryptStream` ⇒ API decrypt-stream NEVER presigned; deny ⇒ coarse PII-free code).
- **WRITE-BACK `POST /party/requests/:id/fulfill`** — **strictly narrower than read:** (1) fail-closed cap gate `documents.on && actions.upload`; (2) **target DERIVED from share scope, NEVER client input** — assert `isDocInShareScope` on the WRITE target (same predicate as read); (3) **inbound-only** — CREATES a new doc attributed `uploaded_by_party = external_share.id` (no `docId` accepted ⇒ no overwrite/דריסה class), party never sets sensitive, one scope entity; (4) auto-association; (5) notify org **via `governOutboundSend` only** (in-app bell direct OK); (6) audit actor = share id, no PII.
- **GATED:** OTP delivery channel — `otpVerified` ships as the structural signal only (default fail-closed `false`); resolver denies sensitive/otp_required until real verification.

### 4.3 — Outbound delivery + autonomous re-chase

- **Ledger generalization** (migration `0083` if 4.1's is renumbered — coordinate; this is the _outbound_ledger_ alter): `proposal_id DROP NOT NULL`; add `cause text NOT NULL DEFAULT 'proposal'` CHECK in(`proposal`,`party_request`,`reminder`); add `party_request_id` FK; CHECK `(cause IN('proposal','reminder') AND proposal_id IS NOT NULL) OR (cause='party_request' AND party_request_id IS NOT NULL)`. **Exactly-once is provably untouched: the idempotency key is `recipientRef:cadenceStep` — `proposal_id` was NEVER in it** (`outbound-governor.ts:151`). Thread `cause?`+`partyRequestId?` through `GovernedSendInput` (default `'proposal'` → all existing callers unchanged); add fail-closed precondition (missing required ref ⇒ `blocked`).
- **Chase send thunk** (`document-chase-executor.ts` — replace the two fail-closed stubs; new collaborator `party-chase-delivery.ts`): resolve recipient from the `party_request`'s `external_share`; **find-or-mint grant via `ExternalSharesService` ceiling-validated path (never raw insert)**; mint `emapp-party` token (4.2); render invite link; **send through `governOutboundSend`** `cause:'party_request'`, `recipientRef=${projectId}:${docType}` (stable M1 key), `cadenceStep`, real `recipientConsented`; on `sent`/`already_sent` flip `party_request.status`→`requested` (idempotent) + notify (reuse `NotificationsService`, PII-free user-framed copy). Provider = the EXISTING `IEmailProvider`/`ISMSProvider` (no new provider).
- **`party-request-chase` recommender** (clone `reminder-cadence.recommender.ts`; register 5th in `proposal-producer.handler.ts:69`): `requested` + bound doc still missing (reuse `detectMissingRequiredDocs`) + last `sent` chase ≥ next cadence offset. Cadence +3/+7/+14, max 3, `cadenceStep` advances ⇒ distinct M1 key. Kind = existing `document.chase.send` (proposeConfirm). **Propose-then-confirm; no auto-execute (outbound ⇒ never auto).**
- **Consent gate (Change 4 — NO #516 bypass):** `resolveChaseRecipientConsent` becomes a real read of `recipient_opt_outs` (#512, unbuilt) keyed by the resolved recipient; **#512 not on main ⇒ `false` unconditionally (fail-closed)**; NEVER hardcode `true`. Two independent gates (kill-switch OFF ⇒ blocked even if consented; consent false ⇒ blocked even if kill-switch on). **Until #512 + creds land, the path is structurally inert.**
- **GATED:** flipping real provider creds + #512 live + per-org real-recipient backfill = the genuine irreversible act. Build all wiring NOW; spec-prove BOTH branches (consent-false→`blocked`, zero ledger rows; test-injected consent-true→`sent`, ledger row `cause='party_request'` `proposal_id IS NULL`, status `requested`, notification emitted). **G-QA OUTCOME:** walk propose→confirm→outcome with consent test-enabled; verify the affected party can actually open the invite link; <1s warm on approve.

---

## 2. FILE-OWNERSHIP / CONFLICT MAP

| Shared resource                                 | Collision risk                                    | Mechanical rule                                                                                                                                                                                                                                                                                                                          |
| ----------------------------------------------- | ------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Migrations + `_journal.json`**                | renumber race (two builders grab the same number) | **ONLY ONE builder holds an open migration at a time. Serialize a migration token.** Order: **0.S5 → 2.4 → 2.5 → 2.6 → 2.7 → 4.1 → 4.3** (one builder per token-hold, internally serial). Non-migration slices run free in parallel. Verify `when` strictly increases each hand-off; **renumber on merge** if another 008x landed first. |
| **`external_share.ts` schema**                  | 4.1/4.2/4.3 all write it; 0.S4 reads its authz    | **Lane L-Party owns it exclusively, hard-serial 4.1→4.2→4.3.** 0.S4 (W0) only READS the resolver and lands BEFORE L-Party opens → no overlap. No other lane writes it.                                                                                                                                                                   |
| **`outbound_ledger.ts`**                        | only 4.3 alters it                                | Last in the serial chain → zero contention.                                                                                                                                                                                                                                                                                              |
| **`he.json` / `en.json`**                       | every FE slice adds keys → merge conflicts        | **Distinct i18n namespace per lane** (P6). 2.3 owns the SWEEP of existing keys — schedule it in its OWN window, NOT concurrent on the same keys as board lanes. Every other FE lane adds ONLY new keys under its own prefix (`board.*`, `party.*`, `data.*`, `states.*`, `party-chase.*`).                                               |
| **`autonomy-policy.ts`** (ACTION_TABLE + kinds) | 1.2b/1.3/2.x/4.x                                  | **L-Spine owns it through 1.2b.** 2.4–2.7 reuse `task.create` ⇒ DO NOT touch it (verify). After 1.2b, kind-adders append disjoint rows (append-only ⇒ trivial rebase); route through 1.0 drift-guard.                                                                                                                                    |
| **`proposals.service.ts:99-185` (executors)**   | 1.2a refactors wholesale                          | **1.2a owns it exclusively. NO other slice touches executors until 1.2a lands.** (Why 1.2a is alone and first on the spine.)                                                                                                                                                                                                             |
| **`projects.service.ts:1404-1410`**             | 0.1 rewrites; 1.1 replaces its caller             | Different regions. Land 0.1 first (soft); 1.1 rebases trivially.                                                                                                                                                                                                                                                                         |
| **`documents-list.client.tsx:327,356`**         | only 3.2 deletes the re-impl                      | No contention.                                                                                                                                                                                                                                                                                                                           |
| **sign-page (`sign/[token]/page.tsx`)**         | **0.S6 AND 2.3 both want the 5xx-as-invalid fix** | **CONFLICT — resolved: sign-page 5xx→retry belongs to 2.3 ONLY** (it owns i18n + sign-page stage split). **0.S6 EXPLICITLY EXCLUDES sign-page** (covers contractor false-empty, portal 5xx→login, docs `error={undefined}`). Brief both builders on the boundary.                                                                        |

**Parallel dispatch waves (what a manager runs concurrently):**

- **Burst A (now, no deps):** `0.1 ∥ 0.2 ∥ 0.3 ∥ 0.S1 ∥ 0.S2 ∥ 0.S3 ∥ 0.S6(minus-sign-page) ∥ 1.0`; `0.S4 ∥ 0.S5` also (0.S5 may grab the migration token first). Read-only red-team audits fan out **unlimited**; **builders capped to 2–3** per host headroom (run `scripts/dev/preflight.sh` — over-spawn crashed the host 3×).
- **Spine (serial, dedicated builder):** `1.1 → 1.2a → 1.2b → 1.5 → 1.3`. Do NOT parallelize within it (same file-region chain).
- **Burst B (after 1.1 lands, disjoint files):** `{2.1, 2.2}` ∥ `{3.1, 3.2}` ∥ `2.3` (own window) ∥ `3.3`. 2.2/3.1 recommender bits wait for 1.2b.
- **State lane (after 1.1+1.2b, one builder, holds migration token):** `2.4 → 2.5 → 2.6 → 2.7`.
- **Party chain (after 1.1, holds external_share exclusively, hard-serial, migration token after the state lane):** `4.1 → 4.2 → 4.3`. Lowest ROI now (P2-1) → schedule LAST; never blocks higher-value bursts.

---

## 3. OWNER-GATED SET (build the wiring NOW behind default-OFF; PREPARE one-click; NEVER perform)

| Gated act                                                    | Slice                                                       | Why genuinely gated                                                                                                                                                           | Ships now (inert)                                                                                                                                               |
| ------------------------------------------------------------ | ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Real email/SMS to real external parties                      | 4.3 send · 4.2 OTP delivery · 1.3 `document.chase` delivery | First-contact recipients have no consent record → `governOutboundSend` ConsentGate fail-closes until `recipient_opt_outs` (#512, unbuilt) + provider creds + kill-switch flip | all token/guard/write-back/ledger/recommender wiring; `delivery_channel='manual_link'`; status flips only on real sends; kill-switch OFF ⇒ every send `blocked` |
| Prod backfill of existing-share recipients                   | 4.1                                                         | mutating live data                                                                                                                                                            | forward migration + dev backfill                                                                                                                                |
| Hosts-file `127.0.0.1 localhost`                             | 0.3                                                         | admin OS write                                                                                                                                                                | Node `ipv4first` flag                                                                                                                                           |
| Per-kind auto-execute flip in prod                           | 1.2b                                                        | turning unattended action ON in prod                                                                                                                                          | full graduation mechanism + per-kind org-opt-in flag (default OFF) built; only the _flip_ waits                                                                 |
| Gate-6 ~750-doc re-encrypt · KMS/R2 provisioning · legal/DPO | pre-existing                                                | irreversible infra/legal                                                                                                                                                      | prepared, not performed (out of scope)                                                                                                                          |

**Honest framing (P0-2):** even with the full spine, the only kinds that _safely_ auto-execute are low-drama internal bookkeeping (`document.autofile`, `project.flagAtRisk`). `signature_request.reissue` CANNOT auto-graduate — its executor `reissueAndDeliver` sends; re-splitting into the dead `reissueExpired` is a regression. **Sell this as "a far better multi-axis proposal engine + a trustworthy undo surface," NOT "unattended autonomy."** Minimal unattended action is the boundary's safety feature, by design.

---

## 4. PER-SLICE GATE (standing bar — EVERY row, in this order)

1. **Code-green** (builder's own: typecheck+lint+tests+self-review). Necessary, never acceptance.
2. **Reuse-verified (P1, LEAD DUTY):** named seam extended, not re-implemented; single-source-of-truth held; no second parallel impl. Flagged in review.
3. **G-RT (independent red-team, UNBOUNDED loop):** find→FP-verify(≥2/3 lenses)→fix→re-run until ZERO confirmed-real, from every angle. NOT the builder. Default-on for every security-sensitive row (all 0.S\*, 1.2b, 2.5, 4.x, authz).
4. **G-QA technophobe real-Chrome walk** (AFTER G-RT closes, owner's real Chrome, AS the role): 5 axes (Network 2xx / URL / Cookies / Redirect / **latency <1s warm, measured ms**) + at-a-glance situation-picture @scale + one-click legibility + legible empty/error/loading + **OUTCOME end-to-end** (affected party actually received it; numbers correct, no "0 מתוך X").
5. **CI green + drift-guard (1.0)** + the slice's named static ratchet.

Per-slice overrides inline above (0.1 residents-legibility DoD; 2.3 e2e-literal grep + view-source; 2.5 `@security-reviewer`; 4.3 OUTCOME-as-recipient + P1-3 CHECK/cause-lookup spec; 3.2 zero-re-impl proof).

**Key files (absolute):** `C:\emapp\packages\jobs\src\autonomy-policy.ts` · `C:\emapp\packages\jobs\src\recommender.ts` · `C:\emapp\packages\db\src\helpers\recommenders\document-chase.recommender.ts` · `C:\emapp\packages\db\src\helpers\recommenders\missing-required-doc.detect.ts` (2.6 sharpens) · `C:\emapp\packages\db\src\helpers\proposal-producer.ts:160` · `C:\emapp\apps\api\src\modules\proposals\proposals.service.ts:99-185` (1.2a extracts) · `C:\emapp\packages\db\src\helpers\perception\project-lifecycle.detect.ts` + `C:\emapp\packages\shared-types\src\project-perception.ts` · `C:\emapp\packages\db\src\helpers\signature-progress.ts` · `C:\emapp\apps\api\src\modules\projects\projects.service.ts:1404-1410` · `C:\emapp\apps\api\src\modules\external-shares\external-party-authz.ts:112` (WIRE, don't rebuild) · `C:\emapp\apps\api\src\modules\contractor-portal\share-token.service.ts` + `contractor-auth.guard.ts` (4.2 clone sources) · `C:\emapp\packages\db\src\schema\external-share.ts` · `C:\emapp\packages\shared-types\src\external-share.ts` · `C:\emapp\packages\db\src\schema\outbound-ledger.ts` · `C:\emapp\packages\db\src\helpers\outbound-governor.ts:151` · `C:\emapp\apps\api\src\modules\proposals\document-chase-executor.ts` · `C:\emapp\packages\config\src\env.ts` · `C:\emapp\apps\web\src\app\[locale]\(dashboard)\_components\situation-picture\board-primitives.tsx` ← delete re-impl `documents-list.client.tsx:327,356` · `C:\emapp\apps\web\src\components\ui\status-badge.tsx` · `C:\emapp\apps\web\src\app-no-bare-text-muted.spec.ts` (ratchet pattern) · `C:\emapp\packages\db\migrations\0079_external_share.sql` (template) + `C:\emapp\packages\db\migrations\meta\_journal.json` · `C:\emapp\docs\DESIGN-external-share-collaboration-redesign.md`.
